language: swift
osx_image: xcode8.3
script:
  - xcodebuild -scheme InstantMockTests -workspace InstantMock.xcworkspace -sdk macosx10.12 build test
after_success:
  - bash <(curl -s https://codecov.io/bash) -J 'InstantMock'